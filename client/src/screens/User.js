import React from "react";
import { View, Text, StyleSheet } from "react-native";

const User = props => {
    return (
        <View>
            <Text>User</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default User;
